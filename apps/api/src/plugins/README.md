# Plugins Fastify (planeado)

- **CORS** — já registado em `app.ts` via `@fastify/cors`
- **JWT** — `@fastify/jwt` instalado; registo em `app.ts` fica para quando as rotas precisarem (sessão pode ficar só Better Auth)
- **Swagger** — documentação OpenAPI quando as rotas estabilizarem
