# Middlewares (planeado)

- **Auth** — validar sessão Better Auth / utilizador autenticado
- **Tenant** — garantir `establishmentId` activo e posse do recurso
- **Rate limit** — proteger rotas públicas (ex.: booking)
