---
description: Enforces import organization standards and path aliases usage for Agenda XPTO API
globs: ["apps/api/src/**/*.ts", "packages/**/*.ts"]
alwaysApply: true
---

# Import Organization Standards - Agenda XPTO API - Version: 1.1.0

## Purpose & Scope
This rule defines import organization for the Agenda XPTO backend, including correct usage of path aliases and standardized import ordering across all TypeScript files. **ALL responses must be provided in Brazilian Portuguese.**

## Import Order
**MUST** follow this exact order with a blank line between each group:

1. **Node.js built-ins** — `node:crypto`, `node:path`, etc.
2. **External libraries** — `fastify`, `zod`, `@prisma/client`, `bullmq`, etc.
3. **Internal packages** — `@agenda-xpto/types`, `@agenda-xpto/validations`
4. **Path aliases** — `~/lib/*`, `~/shared/*`, `~/jobs/*`, `~/modules/*`
5. **Type-only imports** — `import type` ao final de cada grupo ou como grupo separado

> ⚠️ **Imports relativos (`./*`, `../*`) são TOTALMENTE PROIBIDOS.** Sempre use path aliases, inclusive para arquivos do mesmo módulo.

```typescript
// ✅ DO: correct import order with blank lines between groups
import { randomUUID } from 'node:crypto';

import { type FastifyInstance, type FastifyReply, type FastifyRequest } from 'fastify';
import { z } from 'zod';

import type { EstablishmentPublic } from '@agenda-xpto/types';

import { prisma } from '~/lib/prisma';
import { AppError } from '~/shared/errors/AppError';
import { notificationsQueue } from '~/jobs/queues';
import { createAppointmentBodySchema } from '~/modules/appointments/appointments.schema';
import { AppointmentsRepository } from '~/modules/appointments/appointments.repository';
import type { CreateAppointmentBody } from '~/modules/appointments/appointments.schema';
```

## Path Aliases
- **MUST** use path aliases for **todos** os imports, incluindo arquivos do mesmo módulo.
- **NEVER** use imports relativos (`./*` ou `../*`) em qualquer situação.
- **NEVER** misturar imports relativos com aliases.

### Available Aliases
| Alias | Resolves to | Usage |
|---|---|---|
| `~/lib/*` | `apps/api/src/lib/*` | `~/lib/prisma`, `~/lib/redis`, `~/lib/email` |
| `~/shared/*` | `apps/api/src/shared/*` | `~/shared/errors/AppError`, `~/shared/schemas/error.schema` |
| `~/jobs/*` | `apps/api/src/jobs/*` | `~/jobs/queues` |
| `~/modules/*` | `apps/api/src/modules/*` | Para todos os arquivos dentro de módulos |
| `@agenda-xpto/types` | `packages/types/src` | Shared domain types |
| `@agenda-xpto/validations` | `packages/validations/src` | Shared Zod schemas |

```typescript
// ✅ DO: use aliases sempre, inclusive no mesmo módulo
import { prisma } from '~/lib/prisma';
import { AppError } from '~/shared/errors/AppError';
import { notificationsQueue } from '~/jobs/queues';
import { AppointmentsRepository } from '~/modules/appointments/appointments.repository';
import type { EstablishmentPublic } from '@agenda-xpto/types';

// ❌ DON'T: qualquer import relativo é proibido
import { prisma } from '../../lib/prisma';
import { AppError } from '../../../shared/errors/AppError';
import { AppointmentsRepository } from './appointments.repository';
import { createAppointmentBodySchema } from '../appointments/appointments.schema';
```

## Type Imports
- **MUST** use `import type` for all type-only imports.
- **MUST** separate `import type` from value imports.
- **SHOULD** group type imports at the end of their respective group.

```typescript
// ✅ DO: import type for type-only imports
import { z } from 'zod';
import type { ZodSchema } from 'zod';

import { AppError } from '~/shared/errors/AppError';
import type { AppErrorOptions } from '~/shared/errors/AppError';

import { AppointmentsRepository } from '~/modules/appointments/appointments.repository';
import type { CreateAppointmentBody } from '~/modules/appointments/appointments.schema';

// ❌ DON'T: import types as values
import { CreateAppointmentBody } from '~/modules/appointments/appointments.schema'; // type-only, use import type
```

## Named vs Default Imports
- **MUST** use named exports and named imports everywhere — no default exports.
- **NEVER** use `import * as X from` — import only what is needed.

```typescript
// ✅ DO: named imports
import { createAppointmentBodySchema } from '~/modules/appointments/appointments.schema';
import type { CreateAppointmentBody } from '~/modules/appointments/appointments.schema';
import { AppointmentsService } from '~/modules/appointments/appointments.service';

// ❌ DON'T: default imports or namespace imports
import AppointmentsService from '~/modules/appointments/appointments.service'; // no default exports
import * as appointmentsSchema from '~/modules/appointments/appointments.schema'; // import only what you need
```

## Unused Imports
- **NEVER** leave unused imports in any file.
- **MUST** remove imports immediately when the usage is deleted.
- **AVOID** importing entire modules when only one or two exports are needed.

## Module-specific Patterns

### Route files (`*.routes.ts`)
```typescript
import type { FastifyInstance } from 'fastify';

import { AppError } from '~/shared/errors/AppError';
import { createEstablishmentBodySchema } from '~/modules/establishments/establishments.schema';
import { EstablishmentsService } from '~/modules/establishments/establishments.service';
import { EstablishmentsRepository } from '~/modules/establishments/establishments.repository';
import type { CreateEstablishmentBody } from '~/modules/establishments/establishments.schema';
```

### Service files (`*.service.ts`)
```typescript
import { AppError } from '~/shared/errors/AppError';

import type { CreateEstablishmentBody } from '~/modules/establishments/establishments.schema';
import type { EstablishmentsRepository } from '~/modules/establishments/establishments.repository';
```

### Repository files (`*.repository.ts`)
```typescript
import type { Prisma } from '@prisma/client';

import { prisma } from '~/lib/prisma';

import type { CreateEstablishmentBody } from '~/modules/establishments/establishments.schema';
```

### Schema files (`*.schema.ts`)
```typescript
import { z } from 'zod';

import type { EstablishmentPublic } from '@agenda-xpto/types';
```

### Test files (`*.test.ts`)
```typescript
import { describe, it, expect, vi, beforeEach, afterEach } from 'vitest';

import { AppError } from '~/shared/errors/AppError';
import { EstablishmentsService } from '~/modules/establishments/establishments.service';
import type { CreateEstablishmentBody } from '~/modules/establishments/establishments.schema';
import type { EstablishmentsRepository } from '~/modules/establishments/establishments.repository';
```

## Restrictions
- **MUST NOT** use imports relativos (`./*` ou `../*`) em nenhuma situação.
- **MUST NOT** usar `../../` ou qualquer variação de caminho relativo com múltiplos níveis.
- **MUST NOT** import from barrel files (`index.ts`) that re-export everything.
- **MUST NOT** keep unused imports in any file.
- **MUST NOT** use default exports or default imports anywhere.
- **MUST NOT** use `import * as X` — always import specific named exports.

## Related Rules
- `@architecture.mdc`
- `@typescript-conventions.mdc`
- `@testing-tdd.mdc`